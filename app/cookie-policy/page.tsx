export default function CookiePolicyPage() {
  return (
    <section className="panel" style={{ display: 'grid', gap: '.7rem' }}>
      <h1 style={{ fontSize: '2rem' }}>Cookie Policy</h1>
      <p style={{ color: 'var(--muted)' }}>The rebuilt site avoids complex trackers and relies on minimal local browser storage.</p>
      <p style={{ color: 'var(--muted)' }}>By using this site, you accept this essential functionality.</p>
    </section>
  );
}
